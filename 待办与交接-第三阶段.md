# NotEnoughDifficulty 待办与交接（第三阶段）

> 上下文已清理，本文件是新会话的**唯一入口**。读完这一份即可继续工作。
> 上一阶段（移植 + 双 boss + 合成火堆 + 踩坑指南）的成果见文末"已完成"。

---

## 一、当前状态一览（2026-09-13 04:00 核对过磁盘）

| 项 | 状态 |
|---|---|
| 游戏 | StS2 **public beta v0.111.0**（MegaDot/Godot 4.5.1） |
| BaseLib | **v3.4.7**（会被 Steam 自动更新；`_refs\baselib\` 是构建时快照） |
| 本模组 | 编译 **0 错 0 警**，源码 **54 个 .cs**（旧文档写 52，是漏数） |
| 安装 | ✅ **已同步到游戏**（dll/pck/pdb 与 `build\dist` SHA256 一致，pck 于 03:50 重生成） |
| act1~3 双 boss | ✅ 实测生效（合成火堆注入成功：`注入结果 = True`） |
| act4 双 boss | ✅ 生效，但**布局要回滚**（见待办 2） |
| act4 精英 | ✅ **确定名单**：全 12 个精英各一次、按坐标落位、不可能重复（待办 3 已解决） |
| act4 视觉 | 战斗背景按**敌人自己所属的 act** 切 **✅ 实测通过**；地图纹路按层变色 已换到正确挂点（待办 6） |
| act5 | 🔄 **已按新方案重做，待实机验证**（见待办 1 与第四节） |
| 本地化 | ✅ **29 字段 × 2 语言，title + description 全覆盖（0 缺失）**；补上了 `DebugLogging` 与 6 个权重字段的缺失文案；JSON 去掉了 BOM |
| BGM 跨 bank | ✅ 已改 act4 加载全部 bank |
| 日志调试开关 | ✅ 已加 `DebugLogging`（默认关） |
| 踩坑指南 | ✅ `杀戮尖塔2-mod写作踩坑指南.md`（471 行 / 11 章） |

### ⚠️ 旧文档的三处不实，已订正
1. **"编译 0 错 0 警"** —— 上一会话最后一轮 `build65.log` 其实是**失败**的（`CS0051 可访问性不一致: Act5RosterPlan`），改到一半就中断了。本轮已修好，并顺手消掉 `CS8604`（`BossGauntletStylePatches` 给 `EnsureAct4TopBoss` 传可能为 null 的 `state`）。
2. **"源码 52 个 .cs"** —— 实为 54（含本轮新增 `Act5BossDisplay.cs`；新增前是 53）。
3. **"本地化 28/28"** —— 旧脚本只查 `.title`。按 title+description 全查时发现 12 处缺失（`DebugLogging` 的 title/desc + 6 个权重字段的 desc × 2 语言），已补齐。

---

## 二、待办（按优先级）

### 待办 1（最高）：act5 —— **考验 + 极限两档已实现（自己的节点类 + 直线地图）**

**用户最终口径（逐字）**：
> "ACT５只有一个最终BOSS，属于最后面的BOSS，不要套用N10与双重BOSS机制"
> "前面知识恶魔与异鱼都是伪装BOSS，即图标为BOSS图标，战斗为BOSSID的无奖励小怪"
> "我之前不是说了,我们注册一个 直接写一个新类,现在你还是在引用游戏自带的,而不是自己手动去写!去复刻"
> "各显各得"

**现在的实现（本轮完成，编译 0 错 0 警，40 条 patch 目标全部命中）**：

| 节点 | 谁造的 | 图标 | 房间 |
|---|---|---|---|
| 先古之名 | 原版 `NAncientMapPoint`（起点在 `startMapPoints`） | 原版 | Ancient |
| 伪装BOSS ×N | **我们自己的 `Act5BossNode`**（`Act5MidBoss` 在 `SetMap` 后注入） | 各自 encounter 的 BOSS 图标 | `PointType=Monster` → 小怪级奖励、不结束本幕 |
| 火堆（仅极限档） | 原版（`PointType=RestSite`） | 原版 `map_rest` | 原生火堆 |
| 最终BOSS | 原版 `NBossMapPoint`（= 地图 `BossMapPoint`） | `Act.BossEncounter` = 最终BOSS | 原生 BOSS 房（唯一收尾点） |

**两档编排（`Act5BossDisplay.BuildPlan(state, extreme)`，在 `ActBlueprint.BuildAct5` 里、建图之前定稿）**：
- **考验**：先古之名 → 一层随机BOSS → 二层随机BOSS → 最终BOSS（= 三层当前分配的那个）。无火堆。
- **极限**：先古之名 →（没打过的BOSS → 火堆）交替 → 最终BOSS；**取用顺序 一~二~三~一~二~三 循环**，
  **三层那个压轴当最终BOSS**（三层没剩的就退回 `Act3FinalBoss`）；**火堆数 = 场数 − 1**；
  场数上限 6（一列能放 6 场 + 5 个火堆），超出部分日志提示丢弃。
- **判据永远是"战斗ID"**：`RunProgress.GetDefeatedEncounterIds` 扫 `MapPointHistory` 里每场战斗的
  `ModelId.Entry`（**不过滤 RoomType** —— 我们造的伪装BOSS房房间类型被改成 Monster，一过滤就全"没打过"）；
  池子按层取（`GetBaseBossesByAct`，含整幕模组加进来的同层 act 变体）；
  随机源 = `new Rng(state.Rng.Seed, "act5_boss_plan")`（两端一致，不 desync）。

- **`Act5BossNode.cs`（新）**：自己写的节点类，`NMapPoint` 子类，**完全不碰 `NBossMapPoint` 这个类**
  （连场景路径都自己写 `"ui/boss_map_point"` 字面量）。做法与 8 个坑见
  **`杀戮尖塔2-mod写作踩坑指南.md` 第 12 章**。
- **`Act5EncounterPoolPatch.cs`**：`ActModel.PullNextEncounter` 前缀，act5 的 `Monster` 档按进度给第 N 场；
  返回**原型**（`CreateRoom` 自己会 `ToMutable()`，给可变副本会 `MutableModelException` ⇒ 黑屏）。
- **`Act5DisguisedRoomTypePatch.cs`**：`CombatRoom.RoomType => Encounter.RoomType`（IL 实证）⇒
  把伪装BOSS 的房间类型改判成 `Monster`（小怪级奖励）。
- **`Act5DisguisedNoEndingPatch.cs`**：收尾闸门 —— `NRewardsScreen.OnProceedButtonPressed` 前缀，
  只有"当前坐标 == 地图 `BossMapPoint`"的真最终BOSS 才交回原版收尾；伪装BOSS 直接
  `ProceedFromTerminalRewardsScreen()` 关界面回地图（**别压 `_isTerminal`**：那条分支会抛
  `SkipLocalRewardsSet` 异常 ⇒ 前进键按了没反应）。
- **`Act5LinearMap.cs`**：链位（伪装BOSS + 火堆）全部**放进网格**
  （`EnterMapCoordInternal` 会 `GetPoint(coord)` 回查，取不到 ⇒ 黑屏）；
  `SecondBossMapPoint` 不设 ⇒ N10/双 BOSS 机制完全不介入。
- **位置**：整条链在"先古之名 → BOSS"之间等分（x 取先古之名中心）；均分间距 < 节点高×1.15 时
  **把 BOSS 节点往上挪来拉长地图**（拖动无夹紧，最多 3 倍），仍不够才等比缩小节点。
**待验证清单（实机跑 act5 时看这些日志）**：
- `[Act5BossNode] (3,8) 美术子树已搬入 | size=… | 准星=True`
  （`准星=False` 说明 `%SelectionReticle` 没取到，手柄导航时会 Warn）
- `[Act5BossNode] (3,8) 就绪 | 'SOUL_FYSH_BOSS' | 图标来源=贴图|spine骨架`
- `[Act5BossNode] (3,11) 就绪 | 'KNOWLEDGE_DEMON_BOSS' | …` —— **两个图标必须不同**
- `[Act5Mid] (3,8) 撤掉原版建的普通节点 NNormalMapPoint，换成我们自己的 BOSS 节点`
- `[Act5Mid] 下一场伪装BOSS = 'SOUL_FYSH_BOSS'` → 打完再点下一个 → `'KNOWLEDGE_DEMON_BOSS'`
- 点节点后不再有 `NullReferenceException at RunManager.EnterMapCoord_Patch1`（黑屏那次的错）

**⚠️ 本轮实机踩掉的三个坑（都已修，别再犯）**：
1. **黑屏 = `RunManager.EnterMapCoordInternal` 取不到点**。IL：
   ```csharp
   var point = State.Map.GetPoint(coord);                                // 网格里没有 ⇒ null
   return EnterMapPointInternal(coord.row + 1, point.PointType, ...);    // ⇒ NullReferenceException
   ```
   伪装 BOSS 的点**必须放进 `Act5LinearMap.Grid`**（只靠 `AddChildPoint` 串链不行）。
   堆栈显示成 `RunManager.EnterMapCoord_Patch1`（原方法体被 Harmony 挪进 patch 包装帧），
   一度误判成别的 mod —— 扫了 69 个 dll：`ActsFromThePast` 只是**调用** `EnterMapCoord`，没 patch。
   放网格的副作用：`SetMap` 会给网格点先建一个 `NNormalMapPoint`，注入时 `QueueFree()` 撤掉。
2. **点击全被吞 = 照抄了 `if (!IsFocused) return;`**。原版 `NClickableControl` 的
   `_isFocused/_isHovered/_isControllerFocused` 都是 **private**，只有它自己那套 private 悬停
   处理函数会维护；我们自己的节点类走自己的信号接线 ⇒ 这三个状态永远是 false。
   故 `Act5BossNode` 的按压/释放判定只保留 `enabled + 可见 + 左键`。
3. 节点位置必须落在**地图可视范围内**：`NMapPoint.OnRelease` 里有
   `if (!_screen.IsNodeOnScreen(node)) return;`（判据 = 全局 y ∈ (0, 屏幕高)），
   屏幕外的节点点了毫无反应（连投票日志都没有）。位置用原版给网格点的公式
   `(col·distX − 500, −row·distY + 740)`；行号越小越靠下（地图是自下而上铺的），
   两个伪 BOSS 相隔 3 行（581px）才不会叠在一起。

在拍板前，超额名单会被 `LogTruncation` 打警告丢弃。

---

### 待办 2：act4 布局**回滚**（用户明确要求，本轮没做）

**用户原话**："ACT 4 目前没事，回滚代码"

**要回滚什么**：`Act5MapPatch.ConvertAct4NonCombatToRest` 里加的"把扩深行的问号/商店转成火堆"。
用户发现这会导致**正常游玩时后面没有问号与商人**。

**做法**：停用该方法（保留注释说明），让 act4 保持原版地图布局。注意它现在仍在被调用：
```csharp
// Act5/Act5MapPatch.cs 第 39 行附近
if (state.Act is Act5Model) BuildAct5(state);
else if (state.Act is Act4Model) ConvertAct4NonCombatToRest(state);   // ← 这行要停用
```

---

### 待办 3（已解决 2026-09-13）：act4「精英房不足 / 数量不对」→ **改为确定名单**

**用户口径**："地图里面分配的精英是确定的，不是随机的！"、"就９个精英有点离谱了"、"为什么会有精英重复啊？"

**真相（IL 实证）**：base game 是 **4 个 act、每个 3 个精英 = 12 个**，而且**层 1 有两个 act 变体**
（Overgrowth + Underdocks，各 3 个）：
```
层1 Overgrowth : BYGONE_EFFIGY / BYRDONIS / PHROG_PARASITE
层1 Underdocks : PHANTASMAL_GARDENERS / SKULKING_COLONY / TERROR_EEL
层2 Hive       : DECIMILLIPEDE / ENTOMANCER / INFESTED_PRISMS
层3 Glory      : KNIGHTS / MECHA_KNIGHT / SOUL_NEXUS
```
地图战斗位正好 12 个 ⇒ **act4 = 12 个精英各打一遍**。

**两个 bug 都是"把层当身份 / 把进度当目标数"**：
- 目标数用了"未打精英数" ⇒ 打过 3 个精英的那局只出 9 场（用户："９个精英"）；
- 填池用了 `Math.Max(名单, rooms.eliteEncounters.Count)` + `Add(名单[i % 名单.Count])`
  ⇒ 池里有重复项，原版再 `rng.NextItem()` 抽 ⇒ **同一场精英打两遍**（用户："精英重复"）。

**现在的实现（新文件 `ExtraActs/Patches/Act4ElitePlan.cs`）**：
| 环节 | 做法 |
|---|---|
| 名单 | `GetAllBaseElites()`（按 `ActsByIndex` 分层聚合，**含整幕模组塞进同层的 act 变体**），全局去重，顺序确定（层 1→3、act→池） |
| 目标数 | `名单长度`（**不写死 12**）；深度上限也跟着名单走（`ActDepthPatch.MaxRoomsFor`） |
| 落位 | 地图生成后按「离起点由近到远（BFS 深度→列）」把名单逐个钉到战斗房：`Dictionary<MapCoord, EncounterModel>` |
| 取怪 | `ActModel.PullNextEncounter` prefix 按 **`RunState.CurrentMapCoord`** 查表返回（**原版随机抽取完全不参与** ⇒ 不可能重复）；只在"本层是 act4 + 坐标有落位"时接管，否则 `return true` |
| 自愈 | 表以地图实例为键，且**战斗房数量对不上就重建**（读档 / 别的模组改过节点都能自愈） |
| 池子 | `rooms.eliteEncounters` = 名单各一次（供其它逻辑读取，不再取模循环） |

日志：`[Act4] 精英落位: N 个战斗房 ← 名单 M 个 | 由下往上 = A → B → ...`、
`[Act4] 战斗房 (col,row) → 指定精英 'XXX'`、`[ActDepth] 收敛: 深度 N rooms → 战斗房 X ≥ 目标 M`。

**顺手清掉的空转代码**：`MapPointTypeFixupPatch.cs`（act4/act5 分支都只剩一行日志，功能早已搬到
`RebalanceAct4Map`）已删除；`RunProgress.GetUnvisitedElites` 仍保留（诊断用），
`AvoidAdjacentEncounterDuplicate` + `EncounterDeduplicator` 属旧去重思路的遗留（新设计已不可能重复），
**待后续清理**（要同时删配置项与对应本地化文案，单独一轮做）。

---

### 待办 4（低优先）：修被打断的审计脚本

`_tools\check-patch-targets.ps1` 正则可抽取 patch 目标（已复跑：**40 条，类型缺失 0、成员缺失 0**）；
但另写过一个"审计源码是否直接调用非 public 成员"的脚本，**正则写错了**（只抽出 2 个名字，误报 `.FIELD(`）。
如需该审计，重写正则：从 `_api\sts2.api.txt` 里提取 `vis=(private|protected|...)` 那一行的**成员名**
（注意格式是 `METHOD\t签名\tret=…\tstatic=…\tvis=…`）。

---

### 待办 5（低优先）：BGM fallback 常量

`ExtraActs/Patches/CustomActMissingBgmFallbackPatch.cs` 里有硬编码 fallback 曲名。
act4 改成加载全部 bank 后应该不再报错，但常量本身可以换成池子驱动（从 `BgMusicOptions` 里取第一个）。

---

### 待办 6：act4 视觉（**战斗背景 ✅ 已实测通过**；纹路变色 已重写待实机确认）

**用户口径**："ACT ４的地图是荣耀的地图，地图周围的青蓝色纹路变成紫色，遭遇不同层级敌人时，
会根据其层级文字颜色而变色，直到战斗结束回到紫色"、**"不是这样变色啊，是地图边缘的纹理变色"**、
"踩入问号、商店、宝箱、火堆变回紫色，其他时候保持变色颜色直到其变色"。

**① 战斗背景按来源层切（✅ 用户确认 OK）**：`Act4Model.CustomGenerateBackgroundAssets` override，
读 `Act4FightSource.Act`（**敌人自己所属的 act**，由 `NCombatRoom.SetUpBackground` prefix 写入，
`RunProgress.FindActOfEncounter` 反查）→ 返回 `act.GenerateBackgroundAssets(rng)`。
日志：`[Act4] 本场来源 = 'ID' → act 层 N / 'Underdocks'（旅行色 180F24）` +
`[Act4] 战斗背景资产取 'Underdocks'（层 1）`。

⚠️ **必须精确到 act，不能按层取代表 act**：层 1 = Overgrowth + Underdocks，
用代表 act 会让两个子变体一模一样（用户："为什么 ACT 1 的 2 个子变种层级一样的颜色？"）。
地图染色同理：`MapTraveledColor` 色相 —— Overgrowth 33° / Underdocks 266° / Hive 36° / Glory 231°。

**② 地图纹路（条纹）按层变色**：**唯一正确的挂点是 `ActModel.get_MapTopBg/MidBg/BotBg` 这三个 getter**
（IL：`NMapBg.OnVisibilityChanged()` 是这三张贴图的唯一赋值点，每次地图显隐都重新按 act 取图；
三个 rect 的 `Material` 实测全是 null）—— 直接 `rect.Texture = ...` 或改材质参数**必然无效**（详见指南 §3.8）。
实现：`Act4MapStripeTint`（状态：当前层 + 目标色相 + 缓存键片段）→
`ActVisualTheme.ForAct4` 生成底图时读该状态（键 `act4|h<色相>|<layer>`，`RepaintVeinHue` 只旋青蓝纹路的色相）
→ 层变化时 `Traverse.Create(bg).Method("OnVisibilityChanged")` 让原版重取图。
`Act4VisualPatches.ApplyTintForCurrentRoom` 在地图每次打开时按当前房间设定层（非战斗房 = 回紫）。
日志：`[Stripe] 地图纹路 → 层 N，色相 xxx°（重挂 3 张底图）` + `[Theme] 纹路色相 → xxx°，命中 N 像素`。

**③ 仍需清理的硬编码（用户明确要求"不要硬编码"）**：
- act5 布局常量（`Column`、`RepairRows`、`MaxDisguised = 10`）；
- `ActVisualTheme` 的三个调色板（羊皮纸 / 血染 / 紫）集中到一处配置。

### 待办 7（2026-09-13 修完，待实机确认）：act4 背景慢一场 / 暗港配色 / act5 点自己卡死

**① 背景慢一场（已修）**：`[Act4] 战斗背景资产取 'X'` 出现在 `本场来源` **之前** ⇒ 资产生成时机早于
`NCombatRoom.SetUpBackground` 的 prefix。原因：同一个钩子 `EncounterModel.GetBackgroundAssets` 有**两个调用点**，
`EncounterModel.GetAssetPaths`（**预载**）比 `SetUpBackground` 更早。
修法：`Act4FightSource.Current()` 改成**问当前房间**（`RunState.CurrentRoom is CombatRoom → Encounter → FindActOfEncounter`），
静态字段只作兜底。日志应变成"`战斗背景资产取 'Underdocks'`"与本场一致。

**② 暗港配色（已修）**：代码本来是对的（日志：`属于 Underdocks（层 1）色 180F24` → 266°），
但 `MapTraveledColor` 是**近黑 UI 色**，266° 与默认紫 278° 肉眼无差别 ⇒ 看起来"没提取到"。
修法：`ActVisualTheme.SignatureHue(act)` —— 从该 act **自己的地图美术**统计招牌色相（饱和度加权 5° 直方图，峰值桶，按 act 缓存）。
日志：`[Theme] 'Underdocks' 招牌色相 = xxx°（UI 旅行色 266°）`。若某 act 美术主色也落在默认紫附近，需再想办法区分。

**③ act5 点自己卡死（已修）**：`NMapPoint.get_IsTravelable()` 第一分支是
`IsDebugTravelEnabled && !IsTraveling → true`（**绕过 State**）⇒ 调试旅行开着时"自己脚下的点"也能点，
投票去同坐标 → `AddVisitedMapCoord` 返回 false → 房间不重建、票已投出 ⇒ 卡在地图里。
修法：① `Act5BossNode` 只认 `State == MapPointState.Travelable`；
② 新增 `Act5SameCoordClickGuardPatch`（prefix `NMapScreen.OnMapPointSelectedLocally`）——
点到自己所在的坐标就吞掉，不投票（对原版节点如起点/先古节点同样生效）。

**顺带（2026-09-13 已清理）**：`AvoidAdjacentEncounterDuplicate` 配置项 + `NotEnoughDifficultyConfig.Behaviors.cs`、
`ExtraActs/Pool/EncounterDeduplicator.cs`、停用的 `Act5/ActMapOverlay.cs`（雾气）、17 个孤儿 `.uid` 与对应本地化键全部删除；构建 0 错 0 警。

### 待办 8（2026-09-13 已修）：**层号判定去硬编码** —— 模组 act 变体的双 boss 不生效

**用户原话**："你硬编码了双重BOSS？我在模组里面的ACT ２变种没有看到双boss"

**真凶**（`Core/RunProgress.cs` 的 `GetActIndexFromModel`）是一张类型表：
`Underdocks/Overgrowth=>1, Hive=>2, Glory=>3, Act4Model=>4, Act5Model=>5, _=>-1`
⇒ 别的模组的 act 变体一律 **-1** ⇒ `IsDoubleBossEnabled(-1)=false` ⇒ 双 boss 静默失效。
同一个 -1 还连带：双 boss 之间的合成火堆、按层难度强化、act5 名单等**所有** `GetActIndex*` 调用点。

**修法（三个数据口径，依次尝试）**：
1. `ModelDb.ActsByIndex` —— IL 实证其构造是 `foreach (act in Acts) { if (act.Index < 0) continue; buckets[act.Index].Add(act); }`，
   即"层 → 该层全部 act 变体"；base game：Overgrowth/Underdocks=0、Hive=1、Glory=2；
2. `RunState.Acts` 里的位置（整幕模组只 append、不设 Index 的情况；本 mod 的 act4/5 就走这条，
   它们故意 `Index=-1` 以免被游戏当成可随机抽的层）；
3. `act.Index`（act 自报层号）。
三条都认不出 ⇒ **打 warn**（不再静默 -1）；`DoubleBossConfigPatch` 另加一行"每个 act 判成第几层"的诊断。

**验证**：日志 `[DoubleBoss] 本局 act 分层判定：… 2=模组的Act2变体(Index=1) …`，
且 `[DoubleBoss] act2: 首个='X' 第二='Y'`；地图上应出现 `secondBoss=有`。
（用户已确认："双重BOSS兼容成功" ✅）

### 待办 9（2026-09-13 已做，待实机）：兼容 **ACT 4 心脏**（别的模组占第 4 幕时本模组顺延为 5/6）

**用户原话**："得兼容ACT ４心脏啊，加个配置－检测到ACT 4心脏的时候自动将本模组改为ACT 5与ACT 6"

**为什么本来是"碰巧对"的**：游戏 `ActModel.GetRandomList` 遍历 `ActsByIndex` 每一层各取一个 act（IL 实证），
所以用 `CustomActModel(3)` 注册的心脏模组天然排在第四个；本模组是**追加到末尾**的，自然成为 5/6。
但只要下面任一条不对，落位就会错乱 —— 三件事一起做才算兼容：

| 环节 | 实现 |
|---|---|
| ① 永远最后 | `ExpandActListPatch` 先 `RemoveAll(自己)` 再 append（幂等）+ `ActLayout.EnsureOursAreLast()`（run 创建时纠正，防别的 postfix 插在我们后面） |
| ② 配置槽位 | **新增 `Core/ActLayout.cs`**：`ConfigLayerOf(act, state)` —— 本模组的幕恒读 4/5 号开关（与顺延无关），基础三层按绝对层号，别人的额外幕返回 -1（本模组不改别人的幕）。已接入 `DoubleBossConfigPatch` / `SyntheticHearth` / `BossGauntletStylePatches` / `RunProgress.GetHp/DmgMultiplier` |
| ③ 检测留痕 | `ActLayout.Detect()`：`ActsByIndex[3]` 有非本模组的 act → ② `ModelDb.Acts` 里 `Index>=3` → ③ 名字/id 带 heart/心脏（兜底）。日志 `[ActLayout] 检测到第 4 幕已被 'X' 占用 ⇒ 本模组自动顺延为第 5/6 幕` |

**新配置**：`Compat` 区 · `Act4HeartAutoShift`（默认开，中英文案已加，pck 已重打）。
**顺手改**：`if (actIdx == 5) return false;`（传奇幕不夹火堆）→ `state?.Act is Act5Model`（顺延后层号会变，魔法数字漏判）；
清掉本地化里 4 个孤儿键（`*_SPEED_MULTIPLIER`）与一处重复的 `ACT5_MAP.title`（JSON 重复键）。

**验证点**：日志 `Final act list (6): …heart…[#4] -> …Act4Model[#本模组第 5 幕] -> …Act5Model[#本模组第 6 幕]`，
`[ActLayout] 检测到第 4 幕已被 '…' 占用 ⇒ 本模组自动顺延为第 5/6 幕`。

### 待办 10（2026-09-13 已修，待实机）：**背景切换不兼容其他模组的精英**

**用户原话**："背景切换不兼容其他模组的精英"

**真因（IL 实证）**：`EncounterModel.GetBackgroundAssets(ActModel parentAct, Rng rng)` 有两条分支：
```csharp
if (HasCustomBackground) _backgroundAssets = CreateBackgroundAssetsForCustom(rng);   // ★ 自带背景 → 不问 act
else                     _backgroundAssets = parentAct.GenerateBackgroundAssets(rng); // ← 我们原本只挂这条
```
⇒ **自带自定义背景的模组精英**根本不会调用 act 的 getter，挂在 `Act4Model.CustomGenerateBackgroundAssets`
上的切换对它静默失效（日志特征：那场既没有 `战斗背景资产取` 也没有我们的覆盖记录）。
附带：`_backgroundAssets` 是**实例级缓存**（预载 `GetAssetPaths` 也会进来），"只改 act 侧 + 赌时机"本身不稳。

**修法**：
1. **新增 `Act5/Act4BackgroundOverridePatch.cs`**：prefix `EncounterModel.GetBackgroundAssets`（private，字符串名），
   在 act4 里直接用"本场来源 act"的资产作结果（**不写缓存**，顺带免疫实例缓存）；其余一律交回原版。
   日志：`[Act4] 背景覆盖 'X' → 'Underdocks'（该敌人自带背景=True，原版分支会用它的自带背景）`。
2. **`FindActOfEncounter` 放宽**：从"只扫 ActsByIndex 的 1~3 层"改成**扫全部 act**
   （ActsByIndex 各层 + `ModelDb.Acts` 总表，含 `Index < 0` 的追加式 act），
   匹配 `AllEliteEncounters → AllBossEncounters → AllEncounters`，层号用 `GetActIndexFromModel` 数据判定。
3. **新配置**：`Compat` 区 · `Act4_OverrideEncounterBackground`（默认开）：
   开=自带背景的敌人也跟随来源幕；关=尊重那个模组的美术（保留其自带背景）。中英文案 + pck 已更新。

**验证点**：`[Act4] 背景覆盖 '…' → '…'（该敌人自带背景=True/False…）` —— 这行现在每场都会打，
"原本走哪条分支"不再是盲区；若仍有精英不切，把那一场的日志发来即可定位。

**补充（同日第二轮，用户澄清："走得不是模组自带的，而是荣耀三层"）**：
说明失败模式不是"没切换"，而是**切成了错的幕**（基础 act）。根因在归属判定：
旧 `FindActOfEncounter` 按 `ActsByIndex` 顺序取**第一个**命中 —— 基础 act 排在前面，
于是"同时被模组 act 与荣耀挂着的模组精英"被判成 Glory。修法（`Core/RunProgress.cs`）：
1. **先问地图历史**（`MapPointHistory` 外层就是按 act 分组 = 游戏自己记的归属）：
   跳过本模组自己的幕，命中即用 —— 精确、确定、读档也在；
   日志：`[Act4] 本场来源 = 'X' → act 层 N / '模组Act'`；
2. 没记录（该精英在前三层被跳过）→ 池子扫描**收集全部候选**，
   优先**非默认 act**（`IsDefault == false` = 模组/变体 act），并打印候选清单：
   `[RunProgress] 'X' 有 2 个候选 act：Glory(默认) / TheBeyondAct ⇒ 取 'TheBeyondAct'`。
   （基础 act 判定用 `IsDefault`：Glory/Hive/Overgrowth = true；⚠️ 暗港 Underdocks 也是 false，
   所以它不会被"默认"这一条挤到后面 —— 与 3.14 里那个陷阱同源。）
3. 上一轮的 `Act4BackgroundOverridePatch`（截 `EncounterModel.GetBackgroundAssets`）保留：
   它解决的是"自带背景的敌人绕过 act 钩子"，与归属判定正交。

**同日第三轮（用户："不行,还是NO"）—— 找到真正的机制，前面那版是错的方向**：

去创意工坊把对方的代码 dump 出来看（`D:\Steam\steamapps\workshop\content\2868840\3746969593\ActsFromThePast.dll`）：
```
ActsFromThePast.Patches.Acts.ActBackgroundPatches/
  LegacyGenerateBackgroundAssetsPatch : Prefix(ActModel act, ref BackgroundAssets __result)   // act 级：造资产并登记
  LegacyEncounterBackgroundPatch      : Prefix(EncounterModel, ActModel parentAct, Rng, …)   // ★ 敌人级：parentAct 是它的 act 才出它的资产
  LegacyBackgroundCreatePatch         : Prefix(BackgroundAssets bg, ref NCombatBackground __result) // 按 assets **实例引用**查表 → 返回 TheBeyondBackground 节点
```
`TheBeyondBackground : NCombatBackground` 是它自绘的竞技场（几百个图层）。**它的自绘背景只在"parentAct 是它自己的 act"时才挂上**。

⇒ 我上一版"自己造资产"（`__result = source.GenerateBackgroundAssets(rng)`）走不通：
① act 级调用会被 **BaseLib 的 `CustomActModel` 前缀**先截住，它的 act 级钩子根本不跑；
② 即便造出来，也不保证是它 `LegacyBackgrounds` 表里登记的**那个实例**。
结果玩家看到默认场景（= 看起来像荣耀三层）——正是用户的"还是NO"。

**正确做法（新增 `Act5/HomeActBackgroundRedirectPatch.cs`，旧的 Act4BackgroundOverridePatch 已删除）**：
```
Prefix(EncounterModel enc, ref ActModel parentAct, Rng rng, ref BackgroundAssets __result)   // Priority.First
  parentAct 是本模组的幕(act4/act5) 且解析出"敌人自己的幕" ⇒ **parentAct = 那个幕**，return true
  ⇒ 原版与**其它模组的敌人级钩子**照常跑（它们看到的就是正确的那一幕）
```
我们**不改返回值**，只纠正"这是哪一幕"这件事实 —— 别的模组怎么写背景就还是它那套（真兼容）。
唯一例外：`HasCustomBackground` 的敌人默认**尊重其自带背景**（配置项 `Act4_OverrideEncounterBackground`
改成默认**关**，开了才强制按来源幕重造）。日志前缀 `[背景折返] … parentAct Act4Model → 'TheBeyondAct'`。

**同日第四轮（用户："崩溃了"）—— 我引入的自引用无限递归**：
崩溃日志 9.3 MB / 6.8 万行，其中 **42,629 行**是同一组三行在重复：
`战斗背景资产取 'Act4Model'（层 -1）` / `'SOUL_NEXUS_ELITE' 有 3 个候选 act：Glory(默认) / Act4Model / Act5Model ⇒ 取 'Act4Model'` / `认不出 act 'NOTENOUGHDIFFICULTY-ACT4_MODEL' 属于第几层`。

**成因**：本模组的 act4 是**聚合 act**（`AllEncounters` = 第三层所有 act 的并集），
所以任何第三层精英都能在它池子里搜到；而我那条"**优先非默认 act**"的启发式
正好把它（非默认）选中 ⇒ 来源幕 = 自己 ⇒ `source.GenerateBackgroundAssets()` 回到自己的
`CustomGenerateBackgroundAssets` ⇒ 自我递归，且每层都打日志 ⇒ 秒级写爆日志 ⇒ 崩。

**修法（三层，纵深防御）**：
1. `FindActOfEncounter` 的候选**排除 `Act4Model/Act5Model`**（根因）；
2. `Act4FightSource.Current()`：解析结果若是本模组的幕、或 `ReferenceEquals(act, state.Act)` ⇒ 视为无效；
3. `Act4Model.CustomGenerateBackgroundAssets`：`ReferenceEquals(source, this)` 直接走 base；
4. 所有"预载/多次调用"路径的钩子日志加 **key 去重**（保命，防下一次洪泛）。

**验证点**：日志里 `[背景折返] 'ACTSFROMTHEPAST-…'：parentAct Act4Model → 'TheBeyondAct'` 每场一行（不再重复几百次），
且规模正常（一轮几十 KB）。

**同日第五轮（用户："为什么ACT 5/6的boss场景没了?"）—— 折返不分敌人类别**：
上一轮的 `parentAct` 折返对**所有**战斗生效 ⇒ 本幕自己的 BOSS 场景被"来源幕"顶掉
（第 5/6 幕的最终 BOSS 本该用本幕场景，结果换成了那个 BOSS 老家的场景）。
**用户口径**：<i>"检测到处于 ACT 5/4 场景且为精英的时候才会走拦截，否则就放过去"</i>。
修法（两处都加同一道闸，新增 `Act4FightSource.IsEliteFight()`）：
1. `HomeActBackgroundRedirectPatch`：`!IsEliteFight() ⇒ return true`（BOSS / 伪装BOSS=小怪房 / 其它一律放过去）；
2. `Act4Model.CustomGenerateBackgroundAssets`：只有精英才取来源幕，其余走 `base`（本幕默认场景）；
3. `IsEliteFight()` 判据：当前房间的 `Encounter.RoomType == RoomType.Elite`，兜底看 `CurrentMapPoint.PointType`；
   判不出来就**不拦截**（宁可少切，也不能顶掉 BOSS 场景）。

**源码包（2026-09-13）**：已整理并复制到 `C:\Users\34294\Downloads\NotEnoughDifficulty-源码-2026-09-13\`
（同名 `.zip` 也在），内含 `src/`（源码+资源）、`tools/`（MakePck/DumpIl/DumpApi/DumpStrings/FindRef/EnumProbe）、
两份主文档 + `历史文档/`，以及 `README-源码包说明.md`（目录结构 / 离线构建步骤 / 依赖自备说明）。
同时清掉了工作区 `build/` 下 64 个我自己的 `build*.log` 垃圾。

**同日第六轮（用户："对了) 怎么我打一个BOSS直接进建筑师了"）—— 伪装BOSS 误判成 BOSS 房**：

**排查链（全部靠日志 + Cecil 全库扫描）**：
1. `SetLocalPlayerReady`（结束本幕）**全游戏只有一个调用点**：
   `NRewardsScreen.OnProceedButtonPressed` → `ActChangeSynchronizer.SetLocalPlayerReady` → `MoveToNextAct`；
2. 终局分支条件 = `_isTerminal && (CurrentRoom.RoomType == Boss || CurrentRoom.IsVictoryRoom)`，
   而 `IsVictoryRoom` 的 IL = "房间是 EventRoom 且 canonical event 是 `TheArchitect`" ⇒ 战斗房恒 false
   ⇒ 只可能是 **`RoomType == Boss`**；
3. 两个补丁原来都用 `Act5BossDisplay.IsDisguised(encounter)` 判"是不是伪装BOSS"，
   而名单是 `BuildPlan` 按"**当前没打过**的 boss"排的 ⇒ **边打边少**，
   第 N 场之后与实际房间必然错位（日志：房间类型改写 96 次，但"不结束本幕"只有 3 次）。

**修法（两层）**：
1. **判据换成坐标**：新增 `Act5BossDisplay.IsAtFinalBossNode(state)`
   （当前坐标 == `map.BossMapPoint.coord`，判不出来按"是最终BOSS"保守处理）：
   - `Act5DisguisedRoomTypePatch`：act5 里"不在最终BOSS坐标" ⇒ 一律 `RoomType.Monster`；
   - `Act5DisguisedNoEndingPatch`：act5 里"不在最终BOSS坐标" ⇒ 一律不结束本幕（走原版双BOSS中间步）。
2. **名单口径换成"进入本幕之前"**：新增 `RunProgress.GetDefeatedEncounterIdsBeforeCurrentAct(state)`
   （历史本来就是按 act 分组的）⇒ 整幕内名单恒定，读档续玩也一致。

**验证点**：第 5 幕里除最终 BOSS 节点外的每一场，奖励界面按「继续」都应打
`[Act5] 伪装BOSS 奖励界面「继续」→ 不结束本幕…`；只有最终 BOSS 节点打完才结束本幕。

**同日第七轮（用户："我神话的火堆怎么没了?"）—— 名单晚于建图**：

**日志指纹（两处数字对不上）**：
```
[Act5LinearMap] 建图: 网格 7x13 | 链位 1（伪装BOSS 1 + 火堆 0） | BOSS(3,12)   ← 建图时名单是空的
[Act5] BOSS 编排（极限）: 伪装=… 10 个 … | 火堆间隔=True                       ← 名单是建图之后才算的
[Act5Mid] 注入完成: 链位 10（伪装BOSS 10 + 火堆 0）                             ← 地图里没有火堆位
```
`Act5LinearMap` 构造时读 `Act5BossDisplay.DisguisedCount / HearthsBetween` 决定链位数量
（极限档 = 场数 − 1 个火堆）；而名单原本在黑屏窗口（`ActBlueprint`）才排 ⇒ 建图先发生 ⇒ 读到空计划。
后面 `Act5MidBoss` 只能在**已有链位**上注入 BOSS，补不出火堆。

**修法**：`Act5Model.CustomCreateMap` 里**建图前**补一次定稿（幂等）：
```csharp
if (!Act5BossDisplay.HasPlan) Act5BossDisplay.BuildPlan(runState, extreme);
return new Act5LinearMap();
```

**验证点**：进第 5/6 幕时应看到
`[Act5Model] 建图前定稿名单: 伪装BOSS 10 场 | 火堆间隔=True` +
`[Act5LinearMap] 建图: … 链位 19（伪装BOSS 10 + 火堆 9）` +
`[Act5Mid] 注入完成: 链位 19（伪装BOSS 10 + 火堆 9）`。
（**旧存档**里已经建好的那一幕地图不会补火堆 —— 需要重进本幕/新开一局。）

---

## 三、必须知道的硬事实（血泪换来的，别重新踩）

### 3.1 patch 入口要**无条件**先打一行日志

我因缺这一行，浪费了数轮才定位问题。日志里 `[SynthHearth]` 0 次命中时，**无法区分**：
方法没被调用 / 提前 return / 异常被吞。**修法**：入口先打一行 + 结尾打结果。

### 3.2 实例身份：`GenerateRooms(ActModel __instance)` ≠ `state.Act`

实测日志：`__instance==57983585 state.Act==52090220 同一实例=False`，且 `state.Act.normalEncounters=15`（那是 act4 的池）。
**含义**：`GenerateRooms` 跑的时候 `state.Act` 还指向上一层。
**对策**：要给"当前层"设东西，必须**在建图前**（`RunManager.GenerateMap` prefix）用 `state.Act`，不要用 `__instance`。

### 3.3 `MapPointType` 的图标 / 取怪 / 可进入是**三件事**

| 设成 | 图标 | 取怪 |
|---|---|---|
| `Boss` | BOSS | `PullNextEncounter(Boss)` → 直接返回 `RoomSet._boss`（每场同一个） |
| `Monster` | 小怪 | `NextNormalEncounter`（按 `normalEncountersVisited` 顺序） |
| `Elite` | 黄色精英 | `NextEliteEncounter` |

`Boss` 类型节点若没有对应的 `BossMapPoint` 绑定 → **错误图标 + 不可进入**。

### 3.4 换 `RoomSet` 的池 → **必须归零 `*Visited` 计数器**

`normalEncountersVisited` / `eliteEncountersVisited` / `bossEncountersVisited`。不归零 → `visited % Count` 落在错误偏移 → 每场同一个怪。

### 3.5 名单必须**冻结**（用户很满意这点，别改坏）

`Act5RosterPlanner` 以「种子+层+档位」为键缓存名单，建图是第一个调用者。
用户原话："你打过的精英与BOSS都不会重复出现，这个提前构筑地图，缓存挺好的……我很满意"

### 3.6 自定义地图必须提供 `BossMapPoint` / `StartingMapPoint`，且 `Grid` 是 **protected**

- `MapPoint.coord` 是**私有字段、无 setter**，`new MapPoint(col,row)` 会忽略参数 → 靠 `grid[col,row] = p`（+ 反射写 coord）
- ⚠️ **`map.GetAllMapPoints()` 枚举的是 Grid，`BossMapPoint`/`SecondBossMapPoint` 不在里面**
  → 别想"找到第二个 boss 的父节点"（我在这上面失败过多次）
- ✅ act5 的两个 boss 点：`BossMapPoint` 在网格 `(3,1)`；`SecondBossMapPoint` 在**虚拟坐标** `(3,2)`、不进网格
  —— 这与原版 `StandardActMap` 的做法一致（原版第二个 boss 也不在 Grid 里）

### 3.7 合成火堆的锚点

`SyntheticHearth` 需要 `map.SecondBossMapPoint` 来算虚拟坐标（`(Boss.col+50+i, Second.row+50+i)`），
**任何要夹火堆的层都必须有第二 boss 点**，否则注入直接返回 false（日志 `secondBoss=无`）。
另注：`DoubleBossConfigPatch.IsDoubleBossEnabled(actIdx)` **同时是火堆的开关**——只想夹火堆时别忘了它。

---

## 四、构建 / 验证命令（都在工作区内，离线）

```powershell
# 构建（0 错 0 警才算过）
cd 'D:\AI\AI专用工作间\build'; dotnet build

# 重新生成 pck（**改了本地化 JSON 后必须做**）
$W='D:\AI\AI专用工作间'
dotnet "$W\_tools\MakePck\bin\Debug\net9.0\MakePck.dll" `
       "$W\build\dist\NotEnoughDifficulty.pck" 'NotEnoughDifficulty' `
       "$W\build\NotEnoughDifficulty" `
       (Get-ChildItem "$W\build\NotEnoughDifficulty\localization" -Recurse -Filter *.json | % FullName)

# 安装到游戏（**需要工作区外写入权限**；沙箱会以 access denied 拒绝，
# 用 sandbox_permissions=danger-full-access 重试同一条命令即可，本轮已成功）
Copy-Item 'D:\AI\AI专用工作间\build\dist\NotEnoughDifficulty.dll','D:\AI\AI专用工作间\build\dist\NotEnoughDifficulty.pck','D:\AI\AI专用工作间\build\dist\NotEnoughDifficulty.pdb' `
          'D:\Steam\steamapps\common\Slay the Spire 2\mods\NotEnoughDifficulty\' -Force

# patch 目标存在性校验（40 条应全绿）
& 'D:\AI\AI专用工作间\_tools\check-patch-targets.ps1'

# 本地化覆盖校验（**同时查 title 与 description**，应 29 字段 / 全 0 缺失）
# 见本文档第 6 节脚本
```

**游戏日志**：`%APPDATA%\SlayTheSpire2\logs\godot.log`
**要让诊断日志出现**：先打开配置里的 `DebugLogging`（默认关）。

---

## 五、关键文件地图

```
build\NotEnoughDifficultyCode\
├─ Core\
│  ├─ MainFile.cs             入口；MainFile.DebugLog() 诊断日志统一入口
│  ├─ ModCompat.cs            多 mod 检测 + 放权（Harmony.GetPatchInfo 查 owner）
│  ├─ RunProgress.cs          ★ 进度/池子中枢：按层聚合(ActsByIndex)、未打精英/boss、难度公式
│  ├─ RunStateAccessor.cs     反射取 RunManager.State
│  └─ RunStateActIndexWriter.cs 反射写 _currentActIndex（绕过 setter 副作用）
├─ Config\
│  ├─ NotEnoughDifficultyConfig.cs / .Difficulty.cs / .ModelComposition.cs / .Behaviors.cs / .Speed.cs
├─ Act5\
│  ├─ Act5BossDisplay.cs      ★★ 新：唯一口径（第1场=地图BOSS节点 / 第2场=双boss通道 / 上限2）
│  ├─ Act5RosterPlanner.cs    ★ act5 boss 名单（冻结缓存、考验/极限两档）
│  ├─ Act5LinearMap.cs        ★ 自定义 ActMap：只有 Ancient + 1 个 BOSS（+ 虚拟第二 boss 点）
│  ├─ Act5MapPatch.cs         ★ act4 扩深行转换（**待办 2 要停用**）+ act5 池子填充
│  ├─ Act5MidBoss*.cs         旧"中部伪装 boss"三件套（新布局下自动 no-op，留着无副作用）
│  └─ MapPathsDuplicateKeyFixPatch.cs  清 _paths，防 DrawPaths 重复键
├─ ExtraActs\
│  ├─ Bootstrap\ExpandActListPatch.cs  追加 act4/5 到 act 列表
│  ├─ Models\Act4Model.cs / Act5Model.cs
│  └─ Patches\
│     ├─ BossGauntletStylePatches.cs   ★ 10 个 patch（照搬 BossGauntlet 结构）
│     ├─ SyntheticHearthPatch.cs       ★ 合成火堆（虚拟坐标 + NNormalMapPoint.Create）
│     ├─ DoubleBossConfigPatch.cs      按层双 boss 开关 + 确定性选 boss（**act5 恒 true**）
│     ├─ N10DoubleBossAtAct3Patch.cs   N10 双 boss 钉回第 3 层（act5 例外保留）
│     ├─ ActDepthPatch.cs              act4 动态深度
│     ├─ CustomActEncounterReplacementPatch.cs  act4/5 池子填充（act5 两场分配在此）
│     └─ MapPointTypeFixupPatch.cs     act4 Monster→Elite（含 boss 点保护）
├─ Difficulty\DifficultyMultiplierPatches.cs   难度公式（1 + ActFloor×0.1×Y/100）
└─ MultiplayerSync\...                ack-based 配置同步

_refs\          构建引用（游戏/BaseLib dll + 本地 NuGet feed + BossGauntlet.dll 备份）
_api\           程序集元数据转储（含 vis= 访问修饰符；DumpApi 生成）
_tools\         DumpApi / DumpIl / DumpStrings / MakePck / check-patch-targets
_probe\sessions\ 会话记录解压产物（见第九节：以后可以直接翻用户原话）
```

---

## 六、本地化覆盖校验脚本（防漏文案，**已升级为同时查 description**）

```powershell
$W='D:\AI\AI专用工作间'
$fields = Select-String -Path "$W\build\NotEnoughDifficultyCode\Config\*.cs" `
  -Pattern 'public static (bool|double) (\w+) \{ get; set; \}' |
  ForEach-Object { $_.Matches[0].Groups[2].Value } | Sort-Object -Unique
$zhs = Get-Content "$W\build\NotEnoughDifficulty\localization\zhs\settings_ui.json" -Raw -Encoding UTF8 | ConvertFrom-Json
$eng = Get-Content "$W\build\NotEnoughDifficulty\localization\eng\settings_ui.json" -Raw -Encoding UTF8 | ConvertFrom-Json
$miss=0;$desc=0
foreach($f in $fields){
  $snake = ($f -creplace '(?<=[a-z0-9])(?=[A-Z])','_').ToUpper()
  foreach($x in @(@('zhs',$zhs),@('eng',$eng))){
    if(-not $x[1].PSObject.Properties.Name.Contains("NOTENOUGHDIFFICULTY-$snake.title")){ "$($x[0]) 缺 title: $f"; $miss++ }
    if(-not $x[1].PSObject.Properties.Name.Contains("NOTENOUGHDIFFICULTY-$snake.description")){ "$($x[0]) 缺 desc: $f"; $desc++ }
  }
}
"字段 $($fields.Count) 个 | 缺 title $miss | 缺 description $desc"
```

⚠️ **写本地化 JSON 要用无 BOM 的 UTF-8**（`[System.IO.File]::WriteAllText($p,$s,(New-Object System.Text.UTF8Encoding($false)))`）。
`Set-Content -Encoding UTF8` 在 PowerShell 5.1 下会写 BOM，游戏读起来可能有风险。

---

## 七、已完成（不要重做）

- ✅ **【本轮重点】act5 三连 BOSS 改用"自己发房"**（用户方案：不走原版双 boss 槽位）
  - 新增 `Act5/Act5BossSequence.cs`：用 **public** 的 `RunManager.EnterRoomDebug(RoomType.Boss, MapPointType.Boss, encounter, true)`
    自己造 BOSS 房。`CreateRoom` 的 IL 是 `model as EncounterModel ?? Act.PullNextEncounter(roomType)`
    —— **给了 encounter 就直接用它**，所以打谁与地图节点/act 槽位完全无关
  - 三场映射：第 1 场 = 地图 `BossMapPoint`（`RoomSet.Boss`）／第 2 场 = 自己发房／第 3 场 = `SecondBossMapPoint`（终战）
  - 衔接点：`RunManager.ProceedFromTerminalRewardsScreen` 的 prefix 改成 `bool`，
    act5 交给 `Act5BossSequence.TryStartNextFight` 接管（返回 false 跳过原方法）
  - 序号自 `RunState.MapPointHistory` 里数"本层打过的 BOSS 房"恢复 → **读档安全**
  - `Act5BossDisplay.MaxBosses` 由 2 → **3**；`MapBoss` / `InjectedBosses` / `FinalBoss` 三个口径
- ✅ **act4 不再掏空 act3 的 BOSS**：`EnsureAct4TopBoss` 增加动态保底 ——
  act3 的 boss 池只剩 1 个时，act4 的候选池排除它（不硬编码，候选仍来自 `act4.AllBossEncounters`）
- ✅ **N10 双 boss 只留在 act3**：`N10DoubleBossAtAct3Patch` 删掉 `act is not Act5Model` 例外，
  无条件清掉 target 之外所有层的第二 boss
- ✅ **移植**：仓库 1.0.1 → 本机 beta，唯一业务代码改动是 `ConnectedPlayerIds → PlayerIds.ToList()`
- ✅ **构建环境**：完全离线（`_refs\` 自包含 + 本地 NuGet feed），自研 `MakePck` 生成 `.pck`（免装 Godot）
- ✅ **act4/5 自定义 act**：`ExpandActListPatch` 追加到 act 列表；act4 动态深度；act4 全精英
- ✅ **双 boss（act1~4）**：按层开关 + **确定性选 boss**（FNV-1a 散列，不吃 rng）+ 合成火堆夹在中间
- ✅ **合成火堆/商店**：照搬 BossGauntlet 手法（虚拟坐标 +50、`NNormalMapPoint.Create`、`RemovePath` 删直连、`AddChildPoint` 重串、`DrawPaths` 画线、`RecalculateTravelability` 补可通行性）
- ✅ **10 个 patch 面 1:1 对齐 BossGauntlet**
- ✅ **兼容其他模组**：池子按 `ActsByIndex` 按层聚合（不按具体 act 类型）；`ModCompat` 运行时检测 + 放权 + 启动报告
- ✅ **难度模型重构**：`1 + ActFloor × 0.1 × Y/100`（血）与 `× 0.05 × X/100`（攻）
- ✅ **中英双语本地化**：29 字段 × 2 语言，title + description **0 缺失**
- ✅ **日志调试开关** `DebugLogging`（默认关）
- ✅ **踩坑指南** `杀戮尖塔2-mod写作踩坑指南.md`（471 行 / 11 章）
- ✅ **BossGauntlet 拆解笔记** `BossGauntlet-合成节点法-拆解笔记.md`

---

## 七之二、BOSS 图标与房间解析的完整机制（IL 实证，回答"为什么图标显示不出来"）

**① 谁会成为 BOSS 视觉节点 —— 看坐标身份，不看 PointType**
```
NMapScreen.SetMap:
  _bossPointNode       = NBossMapPoint.Create(map.BossMapPoint, ...)        // 只此一个
  _secondBossPointNode = NBossMapPoint.Create(map.SecondBossMapPoint, ...)  // 仅当非 null
  foreach (网格里其余节点) NNormalMapPoint.Create(point, ...)               // 全部走普通节点
```
**整张地图只有 2 个坐标能长出 BOSS 视觉节点。** 在网格里摆 N 个 `MapPointType.Boss` 也没用
——它们一律是 `NNormalMapPoint`（小怪模样）。**这就是旧版"额外 BOSS 节点图标错误/进不去"的机制根源。**

**② 图标画什么 —— 看 act 的"boss 槽位"，不看节点**
```
NBossMapPoint._Ready:
  bool isSecond = (point == map.SecondBossMapPoint);
  EncounterModel e = isSecond ? Act.SecondBossEncounter : Act.BossEncounter;
  if (e.BossNodeSpineResource != null) → 播 Spine 动画（女王/试验体那种会动的）
  else                                 → 贴 e.BossNodePath + ".png" / "_outline.png"（placeholder 占位图）
```
一个 act 只有两个图标来源。**没有"给某个节点单独指定图标"的 API。**

**③ 是不是必须"最后房间"？—— 不是。** 图标只绑槽位，与位置无关。

**④ 点进去打什么 —— 看 PointType + 显式 model**
```
EnterMapPointInternal → RollRoomTypeFor(PointType) → CreateRoom(RoomType, PointType, model)
CreateRoom: model as EncounterModel ?? Act.PullNextEncounter(roomType)   ← 给了 model 就用它
PullNextEncounter: Monster→NextNormalEncounter / Elite→NextElite / Boss→NextBossEncounter(= RoomSet._boss)
```
★ 这就是本 mod 三连 BOSS 的地基：`RunManager.EnterRoomDebug(RoomType.Boss, MapPointType.Boss, encounter, true)`
是 **public** 的，于是可以**自己发一个指定 encounter 的 BOSS 房**，完全绕开地图与槽位。

**⑤ 两槽位的联动规则**（`NRewardsScreen.OnProceedButtonPressed`）
- `SecondBossMapPoint == null` → 打赢 `BossMapPoint` 那场后 `_proceedButton.Disable() + SetLocalPlayerReady()` = 本幕结束
- `!= null` → `CurrentMapCoord == BossMapPoint.coord` 时走 `ProceedFromTerminalRewardsScreen()` 送去第二场
- 所以第二个 boss 点**必须是另一个坐标**（旧版把它放在网格坐标 `(3,2)` 上，就出现了"3:2 女王"那种重叠假象）

---
- ✅ **act5 新方案（本轮）**：`Act5BossDisplay` 口径 + 两节点自定义地图 + 第二场走原生双 boss 通道 + 火堆开关修正 + 文档/文案订正 + 0 错 0 警 + 已安装
- ✅ **反汇编确认原生流程**（本轮）：`NRewardsScreen.OnProceedButtonPressed` / `RunManager.GenerateRooms` 的 IL

---

## 八、已知未解决（供参考，非待办）

| 问题 | 说明 |
|---|---|
| act5 新布局**未实机验证** | 代码与构建已就绪并安装，跑一局看第二节的日志清单 |
| act5 最多 2 场 boss | 原生两条通道的硬限制；想 3+ 必须自造合成 boss 节点（用户要求不做） |
| `map paths` 重复键崩溃 | 已用 transpiler 把 `Dictionary.Add` 换成索引器赋值（幂等） |
| 读档进 act5 时名单不走编排 | `FromSave` 路径不经过 `GenerateRooms`；新方案下**需重新考虑**（两场 boss 由 act 模型持有，理论上读档也保留） |
| 多人联机未实测 | 名单冻结 + 确定性散列已保证理论一致，但没跑过双端 |
| `DeterministicModelHashPatch` 失活 | 日志警告 `no List<(Type,Mod)>.Sort call found … patch is INACTIVE`（base game 可能改过），不影响当前功能 |

---

## 九、会话记录怎么翻（本轮新发现的技巧）

用户的历史原话可以直接从会话记录里挖出来，**比猜需求靠谱**：

```
记录位置：C:\Users\34294\.dsh\sessions\--D-AI-AI~4E13~7528~5DE5~4F5C~95F4--\<会话id>\session.v3.jsonl.zstd
```

这些文件是 **zstd 多帧拼接**（一个文件里 200~2000 个独立 zstd 帧），
`zstdDecompressSync` 只解得出第一帧（= 一行 session 头），所以必须**扫帧魔数 `28 B5 2F FD` 逐帧解压再拼接**：

```powershell
node -e "
const z=require('zlib'),fs=require('fs'),path=require('path');
const root=process.argv[1], out=process.argv[2];
for(const dir of fs.readdirSync(root)){
  const p=path.join(root,dir); if(!fs.statSync(p).isDirectory()) continue;
  for(const f of fs.readdirSync(p)){
    if(!f.endsWith('.zstd')) continue;
    const b=fs.readFileSync(path.join(p,f));
    let offs=[0];
    for(let i=1;i<b.length-3;i++) if(b[i]===0x28&&b[i+1]===0xB5&&b[i+2]===0x2F&&b[i+3]===0xFD) offs.push(i);
    const parts=[];
    for(let k=0;k<offs.length;k++){
      const s=b.slice(offs[k], k+1<offs.length?offs[k+1]:b.length);
      try{ parts.push(z.zstdDecompressSync(s)); }catch(e){}
    }
    fs.writeFileSync(path.join(out, dir+'__'+f.replace('.zstd','')), Buffer.concat(parts));
  }
}
" "<sessions 目录>" "D:\AI\AI专用工作间\_probe\sessions"
```

解压后 `_probe\sessions\*.jsonl` 每行一条事件，`"type":"user/message"` 就是用户原话。
（本轮据此确认了 act5 方案原文，见待办 1。）

---

### 待办 6（新，已定稿口径）：视觉主题 —— act4 / act5 地图背景

**用户原话（逐字，2026-09-13）**：
> "传奇的地图是密林的地图，为羊皮纸风格，金色纹路每局完全随机，用程序做
> 神话的地图为巢穴的地图，周围的黑色变成血液的颜色，浓度与鲜艳不同，地图内部部分则被血液沾染，整体显压抑
> ACT ４的地图是荣耀的地图，地图周围的青蓝色纹路变成紫色，遭遇不同层级敌人时，会根据其层级文字颜色而变色，直到战斗结束回到紫色"

**口径拆解**：
| 档位 | 底图 | 处理 |
|---|---|---|
| act5 · **传奇**（考验档） | **密林**（Overgrowth，act1）的地图背景 | **羊皮纸风格**；**金色纹路**，且**每局完全随机**（→ 用 run seed 生成噪声，程序化） |
| act5 · **神话**（极限档） | **巢穴**（Hive，act2）的地图背景 | 周围黑色 → **血液色**（浓度/鲜艳度有变化）；地图内部**被血液沾染**；整体**压抑** |
| act4 | **荣耀**（Glory，act3）的地图背景 | 周围**青蓝色纹路 → 紫色**；遭遇不同层级敌人时按**该层文字颜色**变色，**战斗结束后回到紫色** |

**已经查清的引擎挂钩（IL 实证）**：
- `ActModel.get_MapTopBg() / get_MapMidBg() / get_MapBotBg()`：**非 virtual**（只有 `public`）⇒
  想换贴图必须 **Harmony postfix 这三个 getter**，对我们自己的 act4/act5 返回
  **运行时生成的 `ImageTexture`**（`Image` 逐像素处理后 `ImageTexture.CreateFromImage`）——
  这样**不需要下载任何素材**，也不必往 pck 里塞图。
- 底图从哪来：`RunProgress.GetRepresentativeAct(layer)?.MapTopBg`（层 1=密林、2=巢穴、3=荣耀）
  —— 直接读原版贴图当输入。
- `ActModel.get_Title()`：**非 virtual**，实现是 `new LocString("acts", Id.Entry + ".title")` ⇒
  改 act5 名字要 patch 这个 getter，按档位返回不同本地化键
  （新增 `...ACT5_MODEL.title_legend` = "传奇" / `..._myth` = "神话"，zhs+eng 都要，另需 `acts.json`）。
- 染色现成旋钮：`ActModel.MapBgColor` / `MapTraveledColor` / `MapUntraveledColor`（只染色、出不了纹路）。
- 遭遇自带背景/音乐：`EncounterModel.CreateBackground` / `HasCustomBackground` / `CustomBgm`
  ⇒ act5 的伪装BOSS **天然**会用自己的背景（和 act4 精英同一套机制，只要 bank 齐了）。

**实施顺序建议**：
1. 先做 act5 改名 + 染色（最小、立刻可见）：patch `get_Title`，两个本地化键，`MapBgColor` 按档位给金/暗红。
2. 再做 act5 两张程序化底图（传奇：密林→羊皮纸+金纹随机；神话：巢穴→黑转血+内部血渍）。
   实现要点：`Image.GetPixel/SetPixel` 逐像素（注意先 `Image.Convert(Format.Rgba8)`）；
   金色纹路用 run seed 决定的噪声/笔画叠加；血渍用低频噪声阈值 + 边缘羽化；结果缓存（每局只算一次，别每帧算）。
3. 最后做 act4：紫纹底图 + "遭遇某层敌人时按该层文字颜色变色，打完回紫"。
   变色时机：战斗开始/结束事件（`CombatManager` 或房间进入/退出）里改 `MapBgColor`/纹路贴图的着色；
   需要先确认"层级文字颜色"取自哪里（`NActBanner` 里 act 名的颜色，或 act 的某个主题色 —— **待读 IL 确认**）。

**⚠️ 性能红线**：贴图处理只在"进入该层/该局第一次"做一次并缓存；`get_MapTopBg` 可能被每帧调用。
